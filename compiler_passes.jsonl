{"pass_a": "ConstantPropagation", "pass_b": "DeadCodeElimination", "ir_snippet": "define i32 @f(i32 %a) {\nentry:\n  %c = add i32 %a, 0\n  ret i32 %c\n}", "label": "safe", "explanation": "Constant propagation folds the add with zero, leaving the same value; dead code elimination can still remove unused instructions."}
{"pass_a": "Mem2Reg", "pass_b": "SROA", "ir_snippet": "define void @g(i32* %p) {\nentry:\n  %v = load i32, i32* %p\n  %inc = add i32 %v, 1\n  store i32 %inc, i32* %p\n  ret void\n}", "label": "pass_a_dominates", "explanation": "Mem2Reg promotes the load/store to a register before SROA can split the aggregate, so Mem2Reg dominates SROA."}
{"pass_a": "LoopUnroll", "pass_b": "InstCombine", "ir_snippet": "define void @h(i32* %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %ptr = getelementptr i32, i32* %a, i32 %i\n  store i32 0, i32* %ptr\n  %next = add i32 %i, 1\n  %cond = icmp slt i32 %next, 4\n  br i1 %cond, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "Loop unrolling changes the loop structure, which may prevent InstCombine from folding the induction variable add."}
{"pass_a": "InlineFunction", "pass_b": "AggressiveDCE", "ir_snippet": "define i32 @callee(i32 %x) {\nentry:\n  %y = mul i32 %x, 2\n  ret i32 %y\n}\n\ndefine i32 @caller(i32 %z) {\nentry:\n  %r = call i32 @callee(i32 %z)\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "Inlining the callee occurs before dead code elimination, allowing AggressiveDCE to remove now\u2011dead code inside the inlined body."}
{"pass_a": "GVN", "pass_b": "InstCombine", "ir_snippet": "define i32 @p(i32 %a, i32 %b) {\nentry:\n  %add1 = add i32 %a, %b\n  %add2 = add i32 %a, %b\n  %cmp = icmp eq i32 %add1, %add2\n  ret i32 %add1\n}", "label": "safe", "explanation": "GVN can deduplicate the two identical adds, but InstCombine can also fold them; they do not interfere."}
{"pass_a": "TailCallElimination", "pass_b": "LoopSimplify", "ir_snippet": "define i32 @rec(i32 %n) {\nentry:\n  %cond = icmp eq i32 %n, 0\n  br i1 %cond, label %base, label %recur\nbase:\n  ret i32 0\nrecur:\n  %dec = sub i32 %n, 1\n  %call = tail call i32 @rec(i32 %dec)\n  ret i32 %call\n}", "label": "pass_b_dominates", "explanation": "LoopSimplify does not affect tail calls, but TailCallElimination runs after LoopSimplify, so LoopSimplify dominates."}
{"pass_a": "InstCombine", "pass_b": "SROA", "ir_snippet": "define i32 @q(i32 %x) {\nentry:\n  %add = add i32 %x, 0\n  %mul = mul i32 %add, 1\n  ret i32 %mul\n}", "label": "safe", "explanation": "InstCombine simplifies arithmetic; SROA works on aggregates, so they operate on disjoint concerns."}
{"pass_a": "DeadCodeElimination", "pass_b": "LoopUnroll", "ir_snippet": "define void @r(i32* %a) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, 2\n  br i1 %cond, label %loop, label %exit\nloop:\n  %ptr = getelementptr i32, i32* %a, i32 %i\n  store i32 0, i32* %ptr\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "Dead code elimination may remove the loop entirely, which changes the unrolling decisions of LoopUnroll."}
{"pass_a": "SROA", "pass_b": "Mem2Reg", "ir_snippet": "define void @s(i32* %arr) {\nentry:\n  %a0 = getelementptr i32, i32* %arr, i32 0\n  %v0 = load i32, i32* %a0\n  %a1 = getelementptr i32, i32* %arr, i32 1\n  %v1 = load i32, i32* %a1\n  %sum = add i32 %v0, %v1\n  ret void\n}", "label": "pass_b_dominates", "explanation": "Mem2Reg promotes loads to registers before SROA can split the array, so Mem2Reg dominates SROA."}
{"pass_a": "AggressiveDCE", "pass_b": "InstCombine", "ir_snippet": "define i32 @t(i32 %x) {\nentry:\n  %add = add i32 %x, 0\n  %unused = mul i32 %add, 2\n  ret i32 %add\n}", "label": "safe", "explanation": "AggressiveDCE removes %unused; InstCombine folds %add with zero. Their actions are independent."}
{"pass_a": "LoopSimplify", "pass_b": "LoopUnroll", "ir_snippet": "define void @u(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "LoopSimplify canonicalizes the loop before LoopUnroll attempts to duplicate its body."}
{"pass_a": "InlineFunction", "pass_b": "GVN", "ir_snippet": "define i32 @add_one(i32 %x) {\nentry:\n  %y = add i32 %x, 1\n  ret i32 %y\n}\n\ndefine i32 @caller(i32 %a, i32 %b) {\nentry:\n  %r1 = call i32 @add_one(i32 %a)\n  %r2 = call i32 @add_one(i32 %b)\n  %sum = add i32 %r1, %r2\n  ret i32 %sum\n}", "label": "pass_a_dominates", "explanation": "Inlining creates duplicate add instructions that GVN can later deduplicate."}
{"pass_a": "InstCombine", "pass_b": "TailCallElimination", "ir_snippet": "define i32 @fact(i32 %n) {\nentry:\n  %cond = icmp eq i32 %n, 0\n  br i1 %cond, label %base, label %recur\nbase:\n  ret i32 1\nrecur:\n  %dec = sub i32 %n, 1\n  %call = tail call i32 @fact(i32 %dec)\n  %mul = mul i32 %n, %call\n  ret i32 %mul\n}", "label": "interferes", "explanation": "InstCombine may fold the tail call into a multiplication pattern that prevents TailCallElimination from recognizing a pure tail call."}
{"pass_a": "Mem2Reg", "pass_b": "InstCombine", "ir_snippet": "define void @v(i32* %p) {\nentry:\n  %load = load i32, i32* %p\n  %add = add i32 %load, 0\n  store i32 %add, i32* %p\n  ret void\n}", "label": "pass_a_dominates", "explanation": "Mem2Reg promotes %load to a register before InstCombine folds the add with zero."}
{"pass_a": "GVN", "pass_b": "AggressiveDCE", "ir_snippet": "define i32 @w(i32 %x) {\nentry:\n  %a = add i32 %x, 5\n  %b = add i32 %x, 5\n  %c = mul i32 %a, %b\n  ret i32 %c\n}", "label": "safe", "explanation": "GVN can deduplicate %a and %b; AggressiveDCE may later remove dead code, but they do not conflict."}
{"pass_a": "LoopUnroll", "pass_b": "LoopSimplify", "ir_snippet": "define void @x(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "If LoopUnroll runs first, the loop body changes and LoopSimplify may not recognize the canonical form."}
{"pass_a": "SROA", "pass_b": "InstCombine", "ir_snippet": "%struct.S = type { i32, i32 }\n\ndefine void @y(%struct.S* %s) {\nentry:\n  %p0 = getelementptr %struct.S, %struct.S* %s, i32 0, i32 0\n  %v0 = load i32, i32* %p0\n  %add = add i32 %v0, 0\n  ret void\n}", "label": "pass_a_dominates", "explanation": "SROA splits the struct before InstCombine folds the add with zero."}
{"pass_a": "AggressiveDCE", "pass_b": "DeadCodeElimination", "ir_snippet": "define i32 @z(i32 %a) {\nentry:\n  %unused = add i32 %a, 0\n  ret i32 %a\n}", "label": "pass_a_dominates", "explanation": "AggressiveDCE removes %unused before the simpler DeadCodeElimination runs."}
{"pass_a": "InlineFunction", "pass_b": "LoopUnroll", "ir_snippet": "define void @inner(i32 %i) {\nentry:\n  %add = add i32 %i, 1\n  ret void\n}\n\ndefine void @outer(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  call void @inner(i32 %i)\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "Inlining the call expands the loop body, which may affect LoopUnroll's unrolling factor."}
{"pass_a": "Mem2Reg", "pass_b": "GVN", "ir_snippet": "define i32 @a(i32 %x) {\nentry:\n  %load = load i32, i32* @g\n  %add = add i32 %load, %x\n  %add2 = add i32 %load, %x\n  ret i32 %add\n}", "label": "safe", "explanation": "Mem2Reg promotes the load; GVN can later deduplicate the two adds. No conflict."}
{"pass_a": "LoopSimplify", "pass_b": "AggressiveDCE", "ir_snippet": "define void @b(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "LoopSimplify creates a canonical loop that may expose dead code for AggressiveDCE."}
{"pass_a": "InstCombine", "pass_b": "GVN", "ir_snippet": "define i32 @c(i32 %x) {\nentry:\n  %add = add i32 %x, 0\n  %add2 = add i32 %x, 0\n  %cmp = icmp eq i32 %add, %add2\n  ret i32 %add\n}", "label": "safe", "explanation": "InstCombine folds both adds; GVN can then see they are identical. No interference."}
{"pass_a": "SROA", "pass_b": "LoopUnroll", "ir_snippet": "%struct.T = type { [4 x i32] }\n\ndefine void @d(%struct.T* %t) {\nentry:\n  %arr = getelementptr %struct.T, %struct.T* %t, i32 0, i32 0\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, 4\n  br i1 %cond, label %loop, label %exit\nloop:\n  %elem = getelementptr [4 x i32], [4 x i32]* %arr, i32 0, i32 %i\n  store i32 0, i32* %elem\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "SROA may split the array into scalars, which changes the loop structure that LoopUnroll expects."}
{"pass_a": "DeadCodeElimination", "pass_b": "InstCombine", "ir_snippet": "define i32 @e(i32 %x) {\nentry:\n  %add = add i32 %x, 0\n  %unused = mul i32 %add, 0\n  ret i32 %add\n}", "label": "safe", "explanation": "InstCombine folds %add; DCE removes %unused. Order does not matter."}
{"pass_a": "GVN", "pass_b": "LoopSimplify", "ir_snippet": "define void @f(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %add = add i32 %i, 0\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "GVN may remove the redundant add, altering the loop header that LoopSimplify analyzes."}
{"pass_a": "InlineFunction", "pass_b": "SROA", "ir_snippet": "define void @inner2(i32* %p) {\nentry:\n  store i32 42, i32* %p\n  ret void\n}\n\ndefine void @outer2(%struct.S* %s) {\nentry:\n  %field = getelementptr %struct.S, %struct.S* %s, i32 0, i32 0\n  call void @inner2(i32* %field)\n  ret void\n}", "label": "pass_a_dominates", "explanation": "Inlining expands the store before SROA can split the struct, so InlineFunction dominates."}
{"pass_a": "LoopUnroll", "pass_b": "AggressiveDCE", "ir_snippet": "define void @g(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "Unrolling may expose dead code that AggressiveDCE later removes."}
{"pass_a": "InstCombine", "pass_b": "LoopSimplify", "ir_snippet": "define void @h(i32 %n) {\nentry:\n  %zero = add i32 0, 0\n  %cond = icmp slt i32 %zero, %n\n  br i1 %cond, label %body, label %exit\nbody:\n  ret void\nexit:\n  ret void\n}", "label": "safe", "explanation": "InstCombine folds the constant zero; LoopSimplify works on the branch condition, no conflict."}
{"pass_a": "Mem2Reg", "pass_b": "AggressiveDCE", "ir_snippet": "define void @i(i32* %p) {\nentry:\n  %load = load i32, i32* %p\n  %add = add i32 %load, 0\n  ret void\n}", "label": "pass_a_dominates", "explanation": "Mem2Reg promotes the load before AggressiveDCE can see that %add is dead."}
{"pass_a": "GVN", "pass_b": "InlineFunction", "ir_snippet": "define i32 @add(i32 %a, i32 %b) {\nentry:\n  %sum = add i32 %a, %b\n  ret i32 %sum\n}\n\ndefine i32 @caller(i32 %x) {\nentry:\n  %r1 = call i32 @add(i32 %x, 5)\n  %r2 = call i32 @add(i32 %x, 5)\n  %total = add i32 %r1, %r2\n  ret i32 %total\n}", "label": "interferes", "explanation": "Inlining creates duplicate adds that GVN could later deduplicate, but the order matters for optimization effectiveness."}
{"pass_a": "LoopSimplify", "pass_b": "TailCallElimination", "ir_snippet": "define i32 @rec2(i32 %n) {\nentry:\n  %cond = icmp eq i32 %n, 0\n  br i1 %cond, label %base, label %recur\nbase:\n  ret i32 0\nrecur:\n  %dec = sub i32 %n, 1\n  %call = tail call i32 @rec2(i32 %dec)\n  ret i32 %call\n}", "label": "pass_a_dominates", "explanation": "LoopSimplify does not affect the tail recursion, but it runs earlier in the pipeline, thus dominates."}
{"pass_a": "SROA", "pass_b": "GVN", "ir_snippet": "%struct.U = type { i32, i32 }\n\ndefine i32 @j(%struct.U* %u) {\nentry:\n  %f0 = getelementptr %struct.U, %struct.U* %u, i32 0, i32 0\n  %v0 = load i32, i32* %f0\n  %f1 = getelementptr %struct.U, %struct.U* %u, i32 0, i32 1\n  %v1 = load i32, i32* %f1\n  %sum = add i32 %v0, %v1\n  ret i32 %sum\n}", "label": "pass_a_dominates", "explanation": "SROA splits the struct fields before GVN can see duplicate loads."}
{"pass_a": "InstCombine", "pass_b": "DeadCodeElimination", "ir_snippet": "define i32 @k(i32 %x) {\nentry:\n  %add = add i32 %x, 0\n  %unused = mul i32 %add, 0\n  ret i32 %add\n}", "label": "safe", "explanation": "InstCombine folds the add; DCE removes the multiplication with zero. Independent."}
{"pass_a": "AggressiveDCE", "pass_b": "LoopUnroll", "ir_snippet": "define void @l(i32 %n) {\nentry:\n  %cond = icmp eq i32 %n, 0\n  br i1 %cond, label %exit, label %loop\nloop:\n  ; empty body\n  br label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "AggressiveDCE removes the empty loop before LoopUnroll attempts to duplicate it."}
{"pass_a": "Mem2Reg", "pass_b": "LoopSimplify", "ir_snippet": "define void @m(i32 %n) {\nentry:\n  %i = alloca i32\n  store i32 0, i32* %i\n  br label %loop\nloop:\n  %val = load i32, i32* %i\n  %cond = icmp slt i32 %val, %n\n  br i1 %cond, label %body, label %exit\nbody:\n  %next = add i32 %val, 1\n  store i32 %next, i32* %i\n  br label %loop\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "Mem2Reg promotes the stack variable to a register, simplifying the loop for LoopSimplify."}
{"pass_a": "GVN", "pass_b": "LoopUnroll", "ir_snippet": "define void @n(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %add = add i32 %i, 0\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "GVN may remove the redundant add, altering the loop body that LoopUnroll expects."}
{"pass_a": "InlineFunction", "pass_b": "InstCombine", "ir_snippet": "define i32 @inc(i32 %x) {\nentry:\n  %y = add i32 %x, 1\n  ret i32 %y\n}\n\ndefine i32 @caller2(i32 %a) {\nentry:\n  %r = call i32 @inc(i32 %a)\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "Inlining creates the add instruction which InstCombine can later fold with other arithmetic."}
{"pass_a": "SROA", "pass_b": "AggressiveDCE", "ir_snippet": "%struct.V = type { i32 }\n\ndefine void @o(%struct.V* %v) {\nentry:\n  %field = getelementptr %struct.V, %struct.V* %v, i32 0, i32 0\n  store i32 0, i32* %field\n  ret void\n}", "label": "safe", "explanation": "SROA splits the struct; AggressiveDCE may later remove the store if the struct is dead. No direct conflict."}
{"pass_a": "DeadCodeElimination", "pass_b": "LoopSimplify", "ir_snippet": "define void @p(i32 %n) {\nentry:\n  %cond = icmp eq i32 %n, 0\n  br i1 %cond, label %exit, label %loop\nloop:\n  br label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "LoopSimplify canonicalizes the loop before DCE removes the unreachable loop body."}
{"pass_a": "InstCombine", "pass_b": "SROA", "ir_snippet": "%struct.W = type { i32 }\n\ndefine void @q(%struct.W* %w) {\nentry:\n  %field = getelementptr %struct.W, %struct.W* %w, i32 0, i32 0\n  %load = load i32, i32* %field\n  %add = add i32 %load, 0\n  ret void\n}", "label": "pass_b_dominates", "explanation": "SROA splits the struct first; InstCombine then folds the add."}
{"pass_a": "AggressiveDCE", "pass_b": "GVN", "ir_snippet": "define i32 @r2(i32 %x) {\nentry:\n  %a = add i32 %x, 5\n  %b = add i32 %x, 5\n  %c = mul i32 %a, %b\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "AggressiveDCE may remove dead uses before GVN attempts to deduplicate identical adds."}
{"pass_a": "LoopUnroll", "pass_b": "InlineFunction", "ir_snippet": "define void @inner3(i32 %i) {\nentry:\n  %add = add i32 %i, 1\n  ret void\n}\n\ndefine void @outer3(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  call void @inner3(i32 %i)\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "Unrolling expands the loop body, which changes the call site for InlineFunction."}
{"pass_a": "Mem2Reg", "pass_b": "LoopUnroll", "ir_snippet": "define void @s2(i32* %a) {\nentry:\n  %i = alloca i32\n  store i32 0, i32* %i\n  br label %loop\nloop:\n  %val = load i32, i32* %i\n  %cond = icmp slt i32 %val, 4\n  br i1 %cond, label %body, label %exit\nbody:\n  %ptr = getelementptr i32, i32* %a, i32 %val\n  store i32 0, i32* %ptr\n  %next = add i32 %val, 1\n  store i32 %next, i32* %i\n  br label %loop\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "Mem2Reg promotes the loop counter, simplifying the loop for LoopUnroll."}
{"pass_a": "GVN", "pass_b": "DeadCodeElimination", "ir_snippet": "define i32 @t2(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = add i32 %x, 0\n  %c = mul i32 %a, %b\n  ret i32 %c\n}", "label": "safe", "explanation": "GVN can deduplicate the adds; DCE may later remove any dead code. No interference."}
{"pass_a": "InlineFunction", "pass_b": "LoopSimplify", "ir_snippet": "define void @inner4(i32 %i) {\nentry:\n  %add = add i32 %i, 1\n  ret void\n}\n\ndefine void @outer4(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  call void @inner4(i32 %i)\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "Inlining changes the loop body, which may affect LoopSimplify's ability to canonicalize the loop."}
{"pass_a": "InstCombine", "pass_b": "AggressiveDCE", "ir_snippet": "define i32 @u2(i32 %x) {\nentry:\n  %add = add i32 %x, 0\n  %unused = mul i32 %add, 0\n  ret i32 %add\n}", "label": "safe", "explanation": "InstCombine folds the add; AggressiveDCE removes the multiplication with zero. Independent."}
{"pass_a": "SROA", "pass_b": "LoopSimplify", "ir_snippet": "%struct.Y = type { [2 x i32] }\n\ndefine void @v2(%struct.Y* %y) {\nentry:\n  %arr = getelementptr %struct.Y, %struct.Y* %y, i32 0, i32 0\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, 2\n  br i1 %cond, label %loop, label %exit\nloop:\n  %elem = getelementptr [2 x i32], [2 x i32]* %arr, i32 0, i32 %i\n  store i32 0, i32* %elem\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "SROA may split the array, altering the loop structure that LoopSimplify expects."}
{"pass_a": "DeadCodeElimination", "pass_b": "GVN", "ir_snippet": "define i32 @w2(i32 %x) {\nentry:\n  %a = add i32 %x, 5\n  %b = add i32 %x, 5\n  %c = mul i32 %a, %b\n  ret i32 %c\n}", "label": "safe", "explanation": "Both passes can operate; DCE does not remove any used code, GVN can deduplicate adds."}
{"pass_a": "LoopSimplify", "pass_b": "InstCombine", "ir_snippet": "define void @x2(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %zero = add i32 0, 0\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "LoopSimplify creates a canonical loop before InstCombine folds the constant zero."}
{"pass_a": "Mem2Reg", "pass_b": "AggressiveDCE", "ir_snippet": "define void @y2(i32* %p) {\nentry:\n  %load = load i32, i32* %p\n  %add = add i32 %load, 0\n  ret void\n}", "label": "pass_a_dominates", "explanation": "Mem2Reg promotes the load before AggressiveDCE can see the dead add."}
{"pass_a": "InlineFunction", "pass_b": "GVN", "ir_snippet": "define i32 @add2(i32 %a, i32 %b) {\nentry:\n  %sum = add i32 %a, %b\n  ret i32 %sum\n}\n\ndefine i32 @caller3(i32 %x) {\nentry:\n  %r1 = call i32 @add2(i32 %x, 5)\n  %r2 = call i32 @add2(i32 %x, 5)\n  %total = add i32 %r1, %r2\n  ret i32 %total\n}", "label": "interferes", "explanation": "Inlining creates duplicate adds that GVN could later deduplicate; order matters for effectiveness."}
{"pass_a": "SROA", "pass_b": "InstCombine", "ir_snippet": "%struct.Z = type { i32 }\n\ndefine void @z2(%struct.Z* %z) {\nentry:\n  %field = getelementptr %struct.Z, %struct.Z* %z, i32 0, i32 0\n  %load = load i32, i32* %field\n  %add = add i32 %load, 0\n  ret void\n}", "label": "pass_a_dominates", "explanation": "SROA splits the struct before InstCombine folds the add with zero."}
{"pass_a": "AggressiveDCE", "pass_b": "LoopSimplify", "ir_snippet": "define void @a2(i32 %n) {\nentry:\n  %cond = icmp eq i32 %n, 0\n  br i1 %cond, label %exit, label %loop\nloop:\n  br label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "AggressiveDCE removes the empty loop before LoopSimplify processes it."}
{"pass_a": "GVN", "pass_b": "InstCombine", "ir_snippet": "define i32 @b2(i32 %x) {\nentry:\n  %add1 = add i32 %x, 0\n  %add2 = add i32 %x, 0\n  %cmp = icmp eq i32 %add1, %add2\n  ret i32 %add1\n}", "label": "safe", "explanation": "InstCombine folds both adds; GVN can then see they are identical. No conflict."}
{"pass_a": "LoopUnroll", "pass_b": "DeadCodeElimination", "ir_snippet": "define void @c2(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "Unrolling may duplicate loop exit code that DCE could later remove, affecting each other's results."}
{"pass_a": "InstCombine", "pass_b": "TailCallElimination", "ir_snippet": "define i32 @fact2(i32 %n) {\nentry:\n  %cond = icmp eq i32 %n, 0\n  br i1 %cond, label %base, label %recur\nbase:\n  ret i32 1\nrecur:\n  %dec = sub i32 %n, 1\n  %call = tail call i32 @fact2(i32 %dec)\n  %mul = mul i32 %n, %call\n  ret i32 %mul\n}", "label": "interferes", "explanation": "InstCombine may transform the multiplication pattern, preventing TailCallElimination from recognizing a pure tail call."}
{"pass_a": "Mem2Reg", "pass_b": "LoopSimplify", "ir_snippet": "define void @d2(i32 %n) {\nentry:\n  %i = alloca i32\n  store i32 0, i32* %i\n  br label %loop\nloop:\n  %val = load i32, i32* %i\n  %cond = icmp slt i32 %val, %n\n  br i1 %cond, label %body, label %exit\nbody:\n  %next = add i32 %val, 1\n  store i32 %next, i32* %i\n  br label %loop\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "Mem2Reg promotes the loop counter, making LoopSimplify's analysis easier."}
{"pass_a": "GVN", "pass_b": "LoopUnroll", "ir_snippet": "define void @e2(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %add = add i32 %i, 0\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "GVN may delete the redundant add, altering the loop body that LoopUnroll expects."}
{"pass_a": "InlineFunction", "pass_b": "InstCombine", "ir_snippet": "define i32 @inc2(i32 %x) {\nentry:\n  %y = add i32 %x, 1\n  ret i32 %y\n}\n\ndefine i32 @caller5(i32 %a) {\nentry:\n  %r = call i32 @inc2(i32 %a)\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "Inlining creates the add instruction before InstCombine can fold it."}
{"pass_a": "SROA", "pass_b": "AggressiveDCE", "ir_snippet": "%struct.A = type { i32 }\n\ndefine void @f2(%struct.A* %a) {\nentry:\n  %field = getelementptr %struct.A, %struct.A* %a, i32 0, i32 0\n  store i32 0, i32* %field\n  ret void\n}", "label": "safe", "explanation": "SROA splits the struct; AggressiveDCE may later remove the store if the struct is unused. No conflict."}
{"pass_a": "DeadCodeElimination", "pass_b": "LoopSimplify", "ir_snippet": "define void @g2(i32 %n) {\nentry:\n  %cond = icmp eq i32 %n, 0\n  br i1 %cond, label %exit, label %loop\nloop:\n  br label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "LoopSimplify canonicalizes the loop before DCE removes unreachable code."}
{"pass_a": "InstCombine", "pass_b": "SROA", "ir_snippet": "%struct.B = type { i32 }\n\ndefine void @h2(%struct.B* %b) {\nentry:\n  %field = getelementptr %struct.B, %struct.B* %b, i32 0, i32 0\n  %load = load i32, i32* %field\n  %add = add i32 %load, 0\n  ret void\n}", "label": "pass_b_dominates", "explanation": "SROA splits the struct first; InstCombine then folds the add."}
{"pass_a": "AggressiveDCE", "pass_b": "GVN", "ir_snippet": "define i32 @i2(i32 %x) {\nentry:\n  %a = add i32 %x, 5\n  %b = add i32 %x, 5\n  %c = mul i32 %a, %b\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "AggressiveDCE may eliminate dead uses before GVN deduplicates identical adds."}
{"pass_a": "LoopUnroll", "pass_b": "InlineFunction", "ir_snippet": "define void @inner5(i32 %i) {\nentry:\n  %add = add i32 %i, 1\n  ret void\n}\n\ndefine void @outer5(i32 %n) {\nentry:\n  %i = phi i32 [0, %entry], [%next, %loop]\n  %cond = icmp slt i32 %i, %n\n  br i1 %cond, label %loop, label %exit\nloop:\n  call void @inner5(i32 %i)\n  %next = add i32 %i, 1\n  br label %entry\nexit:\n  ret void\n}", "label": "interferes", "explanation": "Unrolling expands the loop body, affecting the call site for InlineFunction."}
{"pass_a": "globaldce", "pass_b": "ipsccp", "ir_snippet": "@c = global i32 10, align 4\n\ndefine i32 @func(i32 %a) {\nentry:\n  %tmp = alloca i32, align 4\n  %load = load i32, ptr @c, align 4\n  %add = add i32 %load, %a\n  ret i32 %add\n}\n", "label": "pass_b_dominates", "explanation": "ipsccp propagates the constant 10 for @c, replaces the load with the constant and eliminates @c, so globaldce finds no dead globals to remove."}
{"pass_a": "globaldce", "pass_b": "ipsccp", "ir_snippet": "%S = type { i32, i8 }\n@st = global %S { i32 5, i8 0 }, align 8\n\ndefine i32 @use_struct() {\nentry:\n  %p = getelementptr inbounds %S, ptr @st, i64 0, i32 0\n  %v = load i32, ptr %p, align 4\n  ret i32 %v\n}\n", "label": "pass_b_dominates", "explanation": "ipsccp determines that the first field of @st is the constant 5, folds the load to 5 and deletes @st, so globaldce has no dead global left."}
{"pass_a": "globaldce", "pass_b": "ipsccp", "ir_snippet": "@gptr = global ptr null, align 8\n@val = global i32 42, align 4\n\ndefine i32 @read_val() {\nentry:\n  %load = load i32, ptr @val, align 4\n  ret i32 %load\n}\n", "label": "pass_b_dominates", "explanation": "ipsccp sees that @val is a constant 42, replaces the load with the constant and eliminates @val, thus globaldce finds no unused global to erase."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define void @foo(i8* %src) {\nentry:\n  %dst = alloca i8, align 1\n  %srcptr = bitcast i8* %src to i8*\n  call void @llvm.memcpy.p0i8.p0i8.i64(i8* %dst, i8* %srcptr, i64 8, i1 false)\n  %unused = load i8, i8* %dst, align 1\n  ret void\n}\ndeclare void @llvm.memcpy.p0i8.p0i8.i64(i8* nocapture, i8* nocapture, i64, i1)", "label": "pass_b_dominates", "explanation": "memcpyopt removes the llvm.memcpy call because %dst is never read, so ADCE has nothing left to eliminate."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define void @bar(i32* %src) {\nentry:\n  %dst = alloca i32, align 4\n  %srcptr = bitcast i32* %src to i8*\n  %dstptr = bitcast i32* %dst to i8*\n  call void @llvm.memcpy.p0i8.p0i8.i64(i8* %dstptr, i8* %srcptr, i64 4, i1 false)\n  %val = load i32, i32* %dst, align 4\n  ret void\n}\ndeclare void @llvm.memcpy.p0i8.p0i8.i64(i8* nocapture, i8* nocapture, i64, i1)", "label": "pass_b_dominates", "explanation": "memcpyopt eliminates the memcpy as the stored %dst value is never used, leaving ADCE with no dead code to erase."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define void @baz(i8* %src) {\nentry:\n  %dst = alloca [16 x i8], align 1\n  %srcptr = bitcast i8* %src to i8*\n  %dstptr = getelementptr inbounds [16 x i8], [16 x i8]* %dst, i32 0, i32 0\n  call void @llvm.memcpy.p0i8.p0i8.i64(i8* %dstptr, i8* %srcptr, i64 16, i1 false)\n  %unused = load i8, i8* %dstptr, align 1\n  ret void\n}\ndeclare void @llvm.memcpy.p0i8.p0i8.i64(i8* nocapture, i8* nocapture, i64, i1)", "label": "pass_b_dominates", "explanation": "memcpyopt drops the memcpy because the copied memory is never read, so ADCE finds no dead instructions to remove."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define void @quux(i8* %src) {\nentry:\n  %dst = alloca i8, align 1\n  %srcptr = bitcast i8* %src to i8*\n  call void @llvm.memcpy.p0i8.p0i8.i64(i8* %dst, i8* %srcptr, i64 1, i1 false)\n  %unused = load i8, i8* %dst, align 1\n  ret void\n}\ndeclare void @llvm.memcpy.p0i8.p0i8.i64(i8* nocapture, i8* nocapture, i64, i1)", "label": "pass_b_dominates", "explanation": "memcpyopt eliminates the memcpy because its result (%dst) is never read, leaving ADCE with nothing to eliminate."}
{"pass_a": "dce", "pass_b": "loop-rotate", "ir_snippet": "define void @func1() {\nentry:\n  br label %preheader\n\npreheader:\n  %i0 = phi i32 [0, %entry], [%i1, %latch]\n  %cond = icmp slt i32 %i0, 10\n  br i1 %cond, label %body, label %exit\n\nbody:\n  %unused = add i32 %i0, 0\n  %i1 = add i32 %i0, 1\n  br label %latch\n\nlatch:\n  br label %preheader\n\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "Loop-rotate eliminates the preheader and merges the body, physically deleting the dead %unused add; thus DCE finds no dead code left."}
{"pass_a": "dce", "pass_b": "loop-rotate", "ir_snippet": "define void @func2() {\nentry:\n  br label %ph\n\nph:\n  %i = phi i32 [0, %entry], [%next, %latch]\n  %c = icmp slt i32 %i, 5\n  br i1 %c, label %loop, label %out\n\nloop:\n  %ptr = alloca i32, align 4\n  %deadload = load i32, ptr %ptr, align 4\n  %next = add i32 %i, 1\n  br label %latch\n\nlatch:\n  br label %ph\n\nout:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "Loop-rotate restructures the loop, removing the unreachable %deadload load; after rotation DCE has nothing to eliminate."}
{"pass_a": "dce", "pass_b": "loop-rotate", "ir_snippet": "define void @func3() {\nentry:\n  br label %header\n\nheader:\n  %i0 = phi i32 [0, %entry], [%i1, %latch]\n  %cond = icmp slt i32 %i0, 3\n  br i1 %cond, label %body, label %end\n\nbody:\n  %tmp = call void @noop()\n  %i1 = add i32 %i0, 1\n  br label %latch\n\nlatch:\n  br label %header\n\nend:\n  ret void\n}\n\ndeclare void @noop()", "label": "pass_b_dominates", "explanation": "Loop-rotate folds the loop header, eliminating the dead call %tmp to @noop; consequently DCE performs no work."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f11(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR (output differs from input), after which sccp finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f8(ptr %p) {\nentry:\n  %a = load i32, ptr %p, align 4\n  %b = load i32, ptr %p, align 4\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "safe", "explanation": "gvn and early-cse each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f4(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR (output differs from input), after which jump-threading finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f1(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR (output differs from input), after which loop-unroll finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "lcssa transforms the IR (output differs from input), after which sroa finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR (output differs from input), after which loop-simplify finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f1(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR (output differs from input), after which gvn finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f9(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  store i32 2, ptr %p1, align 4\n  %v0 = load i32, ptr %p0, align 4\n  %v1 = load i32, ptr %p1, align 4\n  %r = add i32 %v0, %v1\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR (output differs from input), after which loop-unroll finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f15(i1 %c, i32 %x, i32 %y) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 %x, ptr %a, align 4\n  br label %merge\nf:\n  store i32 %y, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR (output differs from input), after which reassociate finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define i32 @f4(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR (output differs from input), after which lcssa finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define void @f17(ptr %dst, ptr %src) {\nentry:\n  %tmp = alloca i64, align 8\n  %v = load i64, ptr %src, align 8\n  store i64 %v, ptr %tmp, align 8\n  %out = load i64, ptr %tmp, align 8\n  store i64 %out, ptr %dst, align 8\n  ret void\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR (output differs from input), after which reassociate finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f19(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR (output differs from input), after which sroa finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f2(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and reassociate each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define void @f17(ptr %dst, ptr %src) {\nentry:\n  %tmp = alloca i64, align 8\n  %v = load i64, ptr %src, align 8\n  store i64 %v, ptr %tmp, align 8\n  %out = load i64, ptr %tmp, align 8\n  store i64 %out, ptr %dst, align 8\n  ret void\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR (output differs from input), after which sccp finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f8(ptr %p) {\nentry:\n  %a = load i32, ptr %p, align 4\n  %b = load i32, ptr %p, align 4\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR (output differs from input), after which dce finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f19(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR (output differs from input), after which memcpyopt finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f16(i32 %x, i32 %y) {\nentry:\n  %cmp = icmp sgt i32 %x, %y\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 %y\n}", "label": "safe", "explanation": "jump-threading and loop-simplify each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "licm", "ir_snippet": "define i32 @f16(i32 %x, i32 %y) {\nentry:\n  %cmp = icmp sgt i32 %x, %y\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 %y\n}", "label": "safe", "explanation": "loop-rotate and licm each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f19(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and reassociate each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f20(i32 %x) {\nentry:\n  switch i32 %x, label %def [\n    i32 0, label %case0\n    i32 1, label %case1\n  ]\ncase0:\n  ret i32 10\ncase1:\n  ret i32 20\ndef:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and sccp each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f8(ptr %p) {\nentry:\n  %a = load i32, ptr %p, align 4\n  %b = load i32, ptr %p, align 4\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR (output differs from input), after which aggressive-instcombine finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "tailcallelim", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f20(i32 %x) {\nentry:\n  switch i32 %x, label %def [\n    i32 0, label %case0\n    i32 1, label %case1\n  ]\ncase0:\n  ret i32 10\ncase1:\n  ret i32 20\ndef:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and aggressive-instcombine each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f13(i32 %x) {\nentry:\n  br label %bb1\nbb1:\n  br label %bb2\nbb2:\n  br label %bb3\nbb3:\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR (output differs from input), after which loop-unroll finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR (output differs from input), after which tailcallelim finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "loop-deletion", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f15(i1 %c, i32 %x, i32 %y) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 %x, ptr %a, align 4\n  br label %merge\nf:\n  store i32 %y, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-deletion and loop-idiom each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f4(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR (output differs from input), after which aggressive-instcombine finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "aggressive-instcombine and sroa each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f7(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and instcombine each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f4(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR (output differs from input), after which sroa finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f12(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %r = mul i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR (output differs from input), after which instcombine finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f16(i32 %x, i32 %y) {\nentry:\n  %cmp = icmp sgt i32 %x, %y\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 %y\n}", "label": "safe", "explanation": "loop-unroll and early-cse each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f7(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR (output differs from input), after which loop-simplify finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f9(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  store i32 2, ptr %p1, align 4\n  %v0 = load i32, ptr %p0, align 4\n  %v1 = load i32, ptr %p1, align 4\n  %r = add i32 %v0, %v1\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR (output differs from input), after which tailcallelim finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f19(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and loop-simplify each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f16(i32 %x, i32 %y) {\nentry:\n  %cmp = icmp sgt i32 %x, %y\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 %y\n}", "label": "safe", "explanation": "mem2reg and loop-simplify each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f7(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and early-cse each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR (output differs from input), after which simplifycfg finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define void @f17(ptr %dst, ptr %src) {\nentry:\n  %tmp = alloca i64, align 8\n  %v = load i64, ptr %src, align 8\n  store i64 %v, ptr %tmp, align 8\n  %out = load i64, ptr %tmp, align 8\n  store i64 %out, ptr %dst, align 8\n  ret void\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR (output differs from input), after which lcssa finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and loop-simplify each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "instcombine and sroa each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "gvn and jump-threading each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f13(i32 %x) {\nentry:\n  br label %bb1\nbb1:\n  br label %bb2\nbb2:\n  br label %bb3\nbb3:\n  ret i32 %x\n}", "label": "safe", "explanation": "memcpyopt and aggressive-instcombine each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sroa and gvn each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and loop-unroll each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f3(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR (output differs from input), after which reassociate finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f16(i32 %x, i32 %y) {\nentry:\n  %cmp = icmp sgt i32 %x, %y\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 %y\n}", "label": "safe", "explanation": "aggressive-instcombine and early-cse each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f15(i1 %c, i32 %x, i32 %y) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 %x, ptr %a, align 4\n  br label %merge\nf:\n  store i32 %y, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and tailcallelim each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f20(i32 %x) {\nentry:\n  switch i32 %x, label %def [\n    i32 0, label %case0\n    i32 1, label %case1\n  ]\ncase0:\n  ret i32 10\ncase1:\n  ret i32 20\ndef:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and sccp each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f7(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and dce each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "lcssa", "ir_snippet": "define i32 @f15(i1 %c, i32 %x, i32 %y) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 %x, ptr %a, align 4\n  br label %merge\nf:\n  store i32 %y, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and lcssa each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sccp and mem2reg each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define void @f17(ptr %dst, ptr %src) {\nentry:\n  %tmp = alloca i64, align 8\n  %v = load i64, ptr %src, align 8\n  store i64 %v, ptr %tmp, align 8\n  %out = load i64, ptr %tmp, align 8\n  store i64 %out, ptr %dst, align 8\n  ret void\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR (output differs from input), after which memcpyopt finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "dce", "pass_b": "lcssa", "ir_snippet": "define i32 @f13(i32 %x) {\nentry:\n  br label %bb1\nbb1:\n  br label %bb2\nbb2:\n  br label %bb3\nbb3:\n  ret i32 %x\n}", "label": "safe", "explanation": "dce and lcssa each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f1(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR (output differs from input), after which loop-simplify finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f2(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR (output differs from input), after which early-cse finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define i32 @f20(i32 %x) {\nentry:\n  switch i32 %x, label %def [\n    i32 0, label %case0\n    i32 1, label %case1\n  ]\ncase0:\n  ret i32 10\ncase1:\n  ret i32 20\ndef:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and sccp each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f7(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and sroa each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f4(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  ret i32 %x\n}", "label": "safe", "explanation": "instcombine and adce each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "licm", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f20(i32 %x) {\nentry:\n  switch i32 %x, label %def [\n    i32 0, label %case0\n    i32 1, label %case1\n  ]\ncase0:\n  ret i32 10\ncase1:\n  ret i32 20\ndef:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and loop-rotate each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f16(i32 %x, i32 %y) {\nentry:\n  %cmp = icmp sgt i32 %x, %y\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 %y\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR (output differs from input), after which gvn finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR (output differs from input), after which early-cse finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f19(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR (output differs from input), after which loop-unroll finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f3(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR (output differs from input), after which loop-unroll finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR (output differs from input), after which dce finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and reassociate each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f19(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and mem2reg each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f11(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR (output differs from input), after which loop-simplify finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define void @f14(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and simplifycfg each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define void @f5(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and early-cse each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define void @f14(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR (output differs from input), after which early-cse finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f12(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %r = mul i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR (output differs from input), after which loop-simplify finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f10(i32 %x) {\nentry:\n  %r = tail call i32 @f10(i32 %x)\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "tailcallelim transforms the IR (output differs from input), after which simplifycfg finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f6(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and memcpyopt each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define void @f5(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and memcpyopt each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define i32 @f4(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  ret i32 %x\n}", "label": "safe", "explanation": "adce and dce each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f16(i32 %x, i32 %y) {\nentry:\n  %cmp = icmp sgt i32 %x, %y\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 %y\n}", "label": "safe", "explanation": "loop-idiom and loop-rotate each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "licm", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f19(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and loop-idiom each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define void @f14(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and memcpyopt each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define void @f14(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and dce each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f12(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %r = mul i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR (output differs from input), after which jump-threading finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define i32 @f15(i1 %c, i32 %x, i32 %y) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 %x, ptr %a, align 4\n  br label %merge\nf:\n  store i32 %y, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and early-cse each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define void @f14(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and mem2reg each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "licm", "pass_b": "indvars", "ir_snippet": "define i32 @f20(i32 %x) {\nentry:\n  switch i32 %x, label %def [\n    i32 0, label %case0\n    i32 1, label %case1\n  ]\ncase0:\n  ret i32 10\ncase1:\n  ret i32 20\ndef:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and indvars each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f13(i32 %x) {\nentry:\n  br label %bb1\nbb1:\n  br label %bb2\nbb2:\n  br label %bb3\nbb3:\n  ret i32 %x\n}", "label": "safe", "explanation": "adce and memcpyopt each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and aggressive-instcombine each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f2(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and loop-unroll each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f16(i32 %x, i32 %y) {\nentry:\n  %cmp = icmp sgt i32 %x, %y\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 %y\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f4(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR (output differs from input), after which jump-threading finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define void @f5(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and adce each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define void @f17(ptr %dst, ptr %src) {\nentry:\n  %tmp = alloca i64, align 8\n  %v = load i64, ptr %src, align 8\n  store i64 %v, ptr %tmp, align 8\n  %out = load i64, ptr %tmp, align 8\n  store i64 %out, ptr %dst, align 8\n  ret void\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR (output differs from input), after which adce finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "aggressive-instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f15(i1 %c, i32 %x, i32 %y) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 %x, ptr %a, align 4\n  br label %merge\nf:\n  store i32 %y, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and simplifycfg each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f4(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR (output differs from input), after which memcpyopt finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "loop-rotate", "pass_b": "licm", "ir_snippet": "define i32 @f20(i32 %x) {\nentry:\n  switch i32 %x, label %def [\n    i32 0, label %case0\n    i32 1, label %case1\n  ]\ncase0:\n  ret i32 10\ncase1:\n  ret i32 20\ndef:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-rotate and licm each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f13(i32 %x) {\nentry:\n  br label %bb1\nbb1:\n  br label %bb2\nbb2:\n  br label %bb3\nbb3:\n  ret i32 %x\n}", "label": "safe", "explanation": "loop-unroll and loop-simplify each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sroa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f9(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  store i32 2, ptr %p1, align 4\n  %v0 = load i32, ptr %p0, align 4\n  %v1 = load i32, ptr %p1, align 4\n  %r = add i32 %v0, %v1\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR (output differs from input), after which loop-simplify finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f3(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR (output differs from input), after which loop-simplify finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define void @f17(ptr %dst, ptr %src) {\nentry:\n  %tmp = alloca i64, align 8\n  %v = load i64, ptr %src, align 8\n  store i64 %v, ptr %tmp, align 8\n  %out = load i64, ptr %tmp, align 8\n  store i64 %out, ptr %dst, align 8\n  ret void\n}", "label": "safe", "explanation": "mem2reg and sroa each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f5(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and aggressive-instcombine each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f7(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and adce each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "sroa and loop-unroll both transform the IR but their combined output depends on order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define i32 @f2(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR (output differs from input), after which lcssa finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f15(i1 %c, i32 %x, i32 %y) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 %x, ptr %a, align 4\n  br label %merge\nf:\n  store i32 %y, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR (output differs from input), after which loop-unroll finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define i32 @f19(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR (output differs from input), after which mem2reg finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f3(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR (output differs from input), after which simplifycfg finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "adce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f7(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR (output differs from input), after which adce finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f13(i32 %x) {\nentry:\n  br label %bb1\nbb1:\n  br label %bb2\nbb2:\n  br label %bb3\nbb3:\n  ret i32 %x\n}", "label": "safe", "explanation": "loop-unroll and reassociate each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define i32 @f15(i1 %c, i32 %x, i32 %y) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 %x, ptr %a, align 4\n  br label %merge\nf:\n  store i32 %y, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-deletion and indvars each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR (output differs from input), after which tailcallelim finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f7(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR (output differs from input), after which tailcallelim finds the IR already in its target state and produces no further changes (AB == A)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define i32 @f15(i1 %c, i32 %x, i32 %y) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 %x, ptr %a, align 4\n  br label %merge\nf:\n  store i32 %y, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and adce each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f2(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR (output differs from input), after which jump-threading finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f16(i32 %x, i32 %y) {\nentry:\n  %cmp = icmp sgt i32 %x, %y\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 %y\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define void @f14(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and early-cse each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and memcpyopt each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f12(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %r = mul i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR (output differs from input), after which sccp finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define i32 @f4(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR (output differs from input), after which sroa finds the IR already in its target state and produces no further changes (BA == B)."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f19(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "sccp", "pass_b": "lcssa", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and lcssa each independently transform disjoint parts of the IR, and their combined output is order-independent (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f18(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "lcssa and gvn both transform the IR but their combined output depends on order (AB != BA), indicating conflicting transformations."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "adce and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-idiom and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "safe", "explanation": "instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "interferes", "explanation": "sroa and sccp both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sccp", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-rotate and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sroa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "interferes", "explanation": "mem2reg and sccp both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "loop-unroll and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "mem2reg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-idiom", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-idiom and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "early-cse and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "indvars and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "licm", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "licm and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sroa and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "dce transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "gvn and reassociate both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "sccp and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "sroa and simplifycfg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-idiom", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-idiom and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "gvn and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "safe", "explanation": "gvn and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "jump-threading and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-idiom", "pass_b": "loop-rotate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-idiom and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "gvn and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "indvars and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "loop-unroll and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "dce", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "aggressive-instcombine and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "dce transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "licm", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "licm and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-deletion finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "reassociate and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sroa and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "simplifycfg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-idiom and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "indvars", "pass_b": "loop-rotate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-rotate and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-idiom", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-idiom and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "dce transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "mem2reg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-deletion and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "instcombine and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sroa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sroa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "reassociate and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "indvars", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "indvars and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "interferes", "explanation": "gvn and reassociate both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "adce", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "memcpyopt and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-deletion and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-idiom", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-idiom and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "sroa and lcssa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sccp", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "interferes", "explanation": "instcombine and simplifycfg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "gvn and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "indvars", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "indvars and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "reassociate and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "aggressive-instcombine and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-rotate and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "gvn and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "reassociate and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "simplifycfg and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "indvars", "pass_b": "loop-deletion", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-deletion finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "simplifycfg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "loop-unroll and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-deletion", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-deletion and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-idiom", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-deletion", "pass_b": "loop-rotate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-deletion and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-deletion", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-deletion and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "licm", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "licm and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sccp and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "jump-threading and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "licm", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "licm", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "safe", "explanation": "instcombine and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "reassociate and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "safe", "explanation": "gvn and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "sroa and simplifycfg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "mem2reg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "instcombine and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "instcombine and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "sccp and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "mem2reg and lcssa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sccp", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "loop-simplify and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-deletion and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "reassociate and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-idiom and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "interferes", "explanation": "reassociate and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "gvn and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "loop-simplify and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "gvn and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "instcombine and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "early-cse and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "loop-unroll and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "reassociate and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "safe", "explanation": "reassociate and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "indvars and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "memcpyopt and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "reassociate and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-deletion and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "instcombine and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-idiom", "pass_b": "licm", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-idiom and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-idiom", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "loop-idiom and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-rotate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "indvars and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "loop-unroll and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-rotate", "pass_b": "loop-deletion", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-rotate and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-idiom", "pass_b": "loop-deletion", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-idiom and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "loop-unroll and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "mem2reg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "mem2reg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "loop-simplify and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "reassociate and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-deletion and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "licm", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-rotate and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sroa and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "licm", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-deletion and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "jump-threading and mem2reg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "safe", "explanation": "instcombine and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "loop-unroll and mem2reg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sccp", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "gvn and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "licm", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "lcssa and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-rotate", "pass_b": "loop-deletion", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-rotate and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-deletion finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "simplifycfg and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "dce transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-deletion", "pass_b": "loop-rotate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-deletion and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "gvn and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "aggressive-instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-deletion and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "adce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "mem2reg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "mem2reg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-idiom", "pass_b": "loop-rotate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-idiom and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "sccp and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "sccp and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "interferes", "explanation": "sccp and mem2reg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "licm", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "licm and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "memcpyopt and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-deletion finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "early-cse and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "reassociate and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "dce transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "reassociate and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "licm", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "licm and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-deletion and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "simplifycfg and mem2reg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-rotate", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-rotate and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-idiom and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "reassociate and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-deletion and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "lcssa and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-rotate and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sroa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sccp and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "simplifycfg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "adce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "jump-threading and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "loop-unroll and simplifycfg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "loop-simplify", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "memcpyopt and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "gvn and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-idiom", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-idiom and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "licm", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "indvars and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "licm", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "instcombine and simplifycfg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "memcpyopt and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "indvars", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "indvars and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-idiom", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-idiom and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "reassociate and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "instcombine and reassociate both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "aggressive-instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "tailcallelim and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "lcssa and mem2reg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "lcssa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "jump-threading and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "licm", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "licm and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-deletion and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "indvars and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "interferes", "explanation": "sccp and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "dce transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sccp and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "mem2reg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "instcombine and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "safe", "explanation": "sccp and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sroa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "instcombine and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "indvars and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "indvars", "pass_b": "loop-rotate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sroa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-rotate and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "indvars", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "indvars and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "early-cse and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "loop-unroll and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "instcombine and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-deletion and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "indvars and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "safe", "explanation": "early-cse and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "early-cse and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-idiom and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "indvars and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "safe", "explanation": "gvn and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "safe", "explanation": "early-cse and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-idiom and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "licm", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "indvars", "pass_b": "loop-deletion", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-deletion finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "lcssa and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "memcpyopt", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "gvn and sccp both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "tailcallelim and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "instcombine and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-deletion finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "mem2reg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "gvn and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "loop-simplify and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "licm", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "gvn and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "simplifycfg and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "jump-threading", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "mem2reg and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "loop-unroll and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "interferes", "explanation": "reassociate and mem2reg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "gvn and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-idiom and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "safe", "explanation": "early-cse and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "safe", "explanation": "early-cse and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "instcombine and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "simplifycfg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "interferes", "explanation": "reassociate and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "memcpyopt and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sroa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "reassociate and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "early-cse and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "safe", "explanation": "instcombine and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "jump-threading and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "loop-idiom", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "mem2reg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-rotate and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "tailcallelim and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "instcombine and sccp both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "lcssa and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "jump-threading and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "aggressive-instcombine and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "jump-threading and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "adce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-rotate and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "interferes", "explanation": "early-cse and reassociate both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "jump-threading", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "indvars and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-deletion", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-deletion and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "simplifycfg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-idiom", "pass_b": "licm", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-idiom and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "aggressive-instcombine and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "gvn and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "loop-simplify and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "sroa and sccp both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "safe", "explanation": "instcombine and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "adce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "licm", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "licm and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "gvn and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "safe", "explanation": "reassociate and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-deletion and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-idiom", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-idiom and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sroa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "safe", "explanation": "instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "gvn and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "early-cse and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "indvars", "pass_b": "loop-rotate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "indvars and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-idiom", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "loop-idiom and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-rotate and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-deletion and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sroa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "dce transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-deletion", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-deletion and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "sroa and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "safe", "explanation": "sroa and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "lcssa and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sroa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "instcombine and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "dce transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-rotate", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-rotate and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "instcombine and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "sccp and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "interferes", "explanation": "simplifycfg and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "reassociate and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "indvars and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "reassociate and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "early-cse and lcssa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "simplifycfg and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "reassociate and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sroa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "simplifycfg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-deletion", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-deletion and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "sccp and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sroa and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "licm", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "indvars and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "simplifycfg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "loop-deletion", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-idiom and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "early-cse and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "simplifycfg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "indvars", "pass_b": "loop-deletion", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-deletion finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "instcombine and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "jump-threading", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "safe", "explanation": "sroa and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "interferes", "explanation": "instcombine and reassociate both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "instcombine and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "loop-unroll and simplifycfg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "indvars", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "indvars and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "indvars", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "indvars and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sroa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "instcombine and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "tailcallelim", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "tailcallelim and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "adce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-unroll and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-unroll and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "mem2reg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-idiom and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "dce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "reassociate and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "simplifycfg and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "adce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "loop-rotate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-idiom and loop-rotate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sccp and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "adce transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-rotate", "pass_b": "licm", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-rotate and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "lcssa and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "mem2reg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "instcombine and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-deletion", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-deletion and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-rotate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-deletion", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-deletion finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-rotate and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "aggressive-instcombine and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "jump-threading and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "jump-threading and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-rotate", "pass_b": "loop-deletion", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-rotate and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "jump-threading and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "simplifycfg", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "interferes", "explanation": "reassociate and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "licm", "pass_b": "loop-deletion", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "licm and loop-deletion transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "aggressive-instcombine and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "tailcallelim and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sccp finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "simplifycfg and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "sroa", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "safe", "explanation": "gvn and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "licm", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "licm and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "adce and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "licm", "pass_b": "loop-idiom", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "licm and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-idiom and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "mem2reg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "dce transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "mem2reg and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = mul i32 %x, 3\n  %b = mul i32 %x, 5\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "instcombine and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "early-cse and reassociate both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-rotate and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "gvn and sccp both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "reassociate", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "reassociate and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sccp and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "adce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "memcpyopt and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "jump-threading transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "early-cse and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-simplify", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "reassociate and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "lcssa and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-unroll and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "dce and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "adce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "loop-unroll transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "simplifycfg and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "simplifycfg and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "adce transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sroa", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sroa and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "loop-simplify", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "simplifycfg and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sccp and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "adce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %a, 1\n  %c = sub i32 %b, 0\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "mem2reg", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-idiom", "pass_b": "indvars", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sccp", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "sccp and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sroa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "sroa transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "memcpyopt", "pass_b": "sccp", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and loop-simplify transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "reassociate and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "gvn", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "lcssa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "lcssa and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "memcpyopt", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "memcpyopt and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "sccp and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "gvn and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "simplifycfg and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "memcpyopt and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "tailcallelim", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "memcpyopt and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "tailcallelim", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "tailcallelim and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-deletion", "pass_b": "licm", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-deletion and licm transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "dce transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "gvn and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "gvn and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "sroa and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where mem2reg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "mem2reg", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-rotate", "pass_b": "loop-idiom", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-rotate and loop-idiom transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-simplify and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-simplify and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "dce", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "sccp", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "sccp transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "adce", "pass_b": "dce", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "reassociate and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-idiom", "pass_b": "indvars", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "indvars transforms the IR and leaves it in a state where loop-idiom finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "simplifycfg", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "tailcallelim and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "memcpyopt and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-simplify", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "loop-simplify and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = mul i32 %x, 2\n  %b = add i32 %y, %y\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "reassociate", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where reassociate finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "early-cse and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "gvn and lcssa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "aggressive-instcombine", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "aggressive-instcombine and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "loop-unroll", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "loop-unroll and instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-rotate", "pass_b": "indvars", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "safe", "explanation": "loop-rotate and indvars transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sccp", "pass_b": "adce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "sccp and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where sroa finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "instcombine", "pass_b": "dce", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "instcombine and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "sroa", "pass_b": "simplifycfg", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "sroa and simplifycfg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "sroa", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and sroa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "dce", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "safe", "explanation": "dce and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "aggressive-instcombine", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "jump-threading and aggressive-instcombine transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "mem2reg and lcssa transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "mem2reg and reassociate transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "lcssa", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "pass_a_dominates", "explanation": "lcssa transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "lcssa", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "safe", "explanation": "lcssa and dce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where adce finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "sroa", "pass_b": "aggressive-instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "sroa transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "gvn", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "gvn and sccp both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_b_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where simplifycfg finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "jump-threading", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "jump-threading and loop-unroll transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "adce", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "adce and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "dce", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where dce finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where memcpyopt finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "memcpyopt", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "aggressive-instcombine and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "loop-simplify", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "loop-simplify and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "memcpyopt", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and memcpyopt transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where early-cse finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "mem2reg", "ir_snippet": "define void @f(ptr %a, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, 1\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 8\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "early-cse and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "adce", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "adce and early-cse transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "memcpyopt", "pass_b": "adce", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "safe", "explanation": "memcpyopt and adce transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "tailcallelim", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "gvn", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "early-cse", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where tailcallelim finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "reassociate", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "pass_a_dominates", "explanation": "reassociate transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %dead1 = mul i32 %x, 7\n  %dead2 = add i32 %dead1, 3\n  %dead3 = sub i32 %dead2, 1\n  ret i32 %x\n}", "label": "pass_a_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where lcssa finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "loop-unroll", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "sccp transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "aggressive-instcombine", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_b_dominates", "explanation": "gvn transforms the IR and leaves it in a state where aggressive-instcombine finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "safe", "explanation": "early-cse and jump-threading transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "instcombine", "pass_b": "loop-simplify", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %a = add i32 %x, 1\n  %b = add i32 %a, 2\n  %c = add i32 %b, 3\n  ret i32 %c\n}", "label": "pass_a_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where loop-simplify finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "early-cse", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "pass_b_dominates", "explanation": "early-cse transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "tailcallelim", "pass_b": "sccp", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "safe", "explanation": "tailcallelim and sccp transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "dce", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "dce and mem2reg transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "reassociate", "pass_b": "tailcallelim", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  br i1 true, label %t, label %f\nt:\n  ret i32 %x\nf:\n  ret i32 0\n}", "label": "safe", "explanation": "reassociate and tailcallelim transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "simplifycfg", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "pass_a_dominates", "explanation": "simplifycfg transforms the IR and leaves it in a state where loop-unroll finds nothing to do \u2014 running AB produces the same output as A alone."}
{"pass_a": "jump-threading", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "instcombine transforms the IR and leaves it in a state where jump-threading finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "gvn", "pass_b": "mem2reg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  store i32 42, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "pass_b_dominates", "explanation": "mem2reg transforms the IR and leaves it in a state where gvn finds nothing to do \u2014 running BA produces the same output as B alone."}
{"pass_a": "early-cse", "pass_b": "gvn", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, 0\n  %b = mul i32 %y, 1\n  %r = add i32 %a, %b\n  ret i32 %r\n}", "label": "safe", "explanation": "early-cse and gvn transform disjoint parts of the IR independently \u2014 their combined output is identical regardless of order (AB == BA)."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "jump-threading and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "gvn", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "gvn and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "jump-threading", "pass_b": "sroa", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "jump-threading and sroa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "reassociate", "pass_b": "gvn", "ir_snippet": "define i32 @f(i1 %c, ptr %p) {\nentry:\n  %v = load i32, ptr %p, align 4\n  %w = load i32, ptr %p, align 4\n  %s = add i32 %v, %w\n  br i1 %c, label %t, label %f\nt:\n  ret i32 %s\nf:\n  ret i32 %v\n}", "label": "interferes", "explanation": "reassociate and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "lcssa", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "lcssa and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sroa", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "interferes", "explanation": "sroa and reassociate both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sroa", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "sroa and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(i32 %n) {\nentry:\n  %a = alloca i32, align 4\n  store i32 0, ptr %a, align 4\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %v = load i32, ptr %a, align 4\n  %v2 = add i32 %v, 1\n  store i32 %v2, ptr %a, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  %r = load i32, ptr %a, align 4\n  ret i32 %r\n}", "label": "interferes", "explanation": "early-cse and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "instcombine and simplifycfg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "sroa and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "sroa", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca { i32, i32 }, align 8\n  %p0 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 0\n  %p1 = getelementptr inbounds { i32, i32 }, ptr %a, i32 0, i32 1\n  store i32 1, ptr %p0, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 2, ptr %p1, align 4\n  br label %merge\nf:\n  store i32 3, ptr %p1, align 4\n  br label %merge\nmerge:\n  %x = load i32, ptr %p0, align 4\n  %y = load i32, ptr %p1, align 4\n  %r = add i32 %x, %y\n  ret i32 %r\n}", "label": "interferes", "explanation": "sroa and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "sccp", "ir_snippet": "define i32 @f(i32 %x) {\nentry:\n  %cmp = icmp eq i32 %x, %x\n  br i1 %cmp, label %t, label %f\nt:\n  ret i32 1\nf:\n  ret i32 0\n}", "label": "interferes", "explanation": "early-cse and sccp both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "loop-unroll", "pass_b": "early-cse", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "loop-unroll and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "simplifycfg", "pass_b": "loop-unroll", "ir_snippet": "define void @f(ptr %a) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  store i32 %i, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, 4\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "simplifycfg and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define i32 @f(i32 %x, i32 %y) {\nentry:\n  %a = add i32 %x, %y\n  %b = add i32 %x, %y\n  %c = mul i32 %a, 2\n  %d = mul i32 %b, 3\n  %r = add i32 %c, %d\n  ret i32 %r\n}", "label": "interferes", "explanation": "early-cse and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "loop-unroll", "pass_b": "gvn", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "loop-unroll and gvn both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "mem2reg", "pass_b": "jump-threading", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "mem2reg and jump-threading both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %dst, ptr %src, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %ps = getelementptr inbounds i32, ptr %src, i32 %i\n  %pd = getelementptr inbounds i32, ptr %dst, i32 %i\n  %v = load i32, ptr %ps, align 4\n  store i32 %v, ptr %pd, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "instcombine and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "gvn", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "gvn and lcssa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "early-cse and lcssa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "mem2reg", "pass_b": "simplifycfg", "ir_snippet": "define i32 @f(i1 %c) {\nentry:\n  %a = alloca i32, align 4\n  br i1 %c, label %t, label %f\nt:\n  store i32 1, ptr %a, align 4\n  br label %merge\nf:\n  store i32 2, ptr %a, align 4\n  br label %merge\nmerge:\n  %v = load i32, ptr %a, align 4\n  ret i32 %v\n}", "label": "interferes", "explanation": "mem2reg and simplifycfg both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "lcssa", "pass_b": "instcombine", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "lcssa and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "instcombine and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "mem2reg", "pass_b": "reassociate", "ir_snippet": "define i32 @f(ptr %p) {\nentry:\n  %a = alloca i32, align 4\n  store i32 5, ptr %a, align 4\n  %v = load i32, ptr %a, align 4\n  %r = add i32 %v, %v\n  ret i32 %r\n}", "label": "interferes", "explanation": "mem2reg and reassociate both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "instcombine", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "early-cse and instcombine both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "early-cse", "ir_snippet": "define void @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %pa = getelementptr inbounds i32, ptr %a, i32 %i\n  %pb = getelementptr inbounds i32, ptr %b, i32 %i\n  %va = load i32, ptr %pa, align 4\n  %vb = load i32, ptr %pb, align 4\n  %s = add i32 %va, %vb\n  store i32 %s, ptr %pa, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret void\n}", "label": "interferes", "explanation": "instcombine and early-cse both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "instcombine", "pass_b": "lcssa", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "instcombine and lcssa both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
{"pass_a": "early-cse", "pass_b": "loop-unroll", "ir_snippet": "define i32 @f(ptr %a, ptr %b, i32 %n) {\nentry:\n  br label %loop\nloop:\n  %i = phi i32 [ 0, %entry ], [ %i.next, %loop ]\n  %inv = load i32, ptr %b, align 4\n  %p = getelementptr inbounds i32, ptr %a, i32 %i\n  %v = load i32, ptr %p, align 4\n  %s = add i32 %v, %inv\n  store i32 %s, ptr %p, align 4\n  %i.next = add nsw i32 %i, 1\n  %cmp = icmp slt i32 %i.next, %n\n  br i1 %cmp, label %loop, label %exit\nexit:\n  ret i32 %inv\n}", "label": "interferes", "explanation": "early-cse and loop-unroll both modify the IR but their combined output depends on execution order (AB != BA), indicating conflicting transformations."}
